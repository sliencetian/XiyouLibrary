<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/theme_color"
    android:clipToPadding="true"
    android:fitsSystemWindows="true" >

    <include
        android:id="@+id/actionbar_activity_advice"
        layout="@layout/actionbar" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/actionbar_activity_advice"
        android:background="@color/white"
        android:orientation="vertical" >

        <EditText
            android:id="@+id/et_activity_advice"
            android:layout_width="fill_parent"
            android:layout_height="150dp"
            android:background="@color/Light_grey"
            android:gravity="top"
            android:hint="@string/advice_text"
            android:padding="2dp"
            android:textSize="15sp" />
    </LinearLayout>

    <Button
        android:id="@+id/bt_activity_advice"
        android:layout_width="80dp"
        android:layout_height="@dimen/actionbar_height"
        android:layout_alignParentRight="true"
        android:background="@drawable/bt_selector"
        android:text="@string/bt_send_text"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size" />

</RelativeLayout>